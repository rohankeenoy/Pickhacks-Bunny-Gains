using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class buySell : MonoBehaviour
{
    // create an instance of class
    //public static cash instance;
    // Start is called before the first frame update
    public static cash instance;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    //assigns cost of bread randomly
    public void breadCost(){
        //bread is randomly assigned different prices
        //users want to buy low
    }


    public void sellTheBread()
    {
        //subtract the users bread
        //add the users money
    }

    public void sellTheVeggies()
    {
        //subtract the users veggies
        // add the users money (+5)
        //cash.instance.addMoney(var4money)
    }

}
